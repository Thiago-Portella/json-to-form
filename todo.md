todos os bugs ainda não resolvidos no projeto estarão aqui.

- Quando o nome de algum campo é trocado, seu equivalente não reflete em botões. Isso deve ocorrer imediatamente após a troca, incluindo todos os caminhos em que esse está envolvido.
    Exemplo: caminho "a.b.c". Se eu troco b para g, todos os botões que mencionavam "abc", devem passar a mencionar "a.g.c".
- Quando clico no botão canselar na tela de novo nome de campo, algum bug ocorre e a página é reiniciada.
- quando troco o nome de um item boleano, seu novo nome não reflete imediatamente na caixa de seleção
- quando tento trocar o nome de um campo mais de uma vez, algum bug ocorre na segunda tentativa e a página recarrega
- quando tento trocar o nome de uma lista, um bug ocorre a página é reiniciada
- quando troco o nome de um objeto e tento trocar o nome de qualquer item dentro deste, o campo para troca aparece, mas ao executar, um bug ocorre e a página é reiniciada.